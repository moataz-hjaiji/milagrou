/**
 * @swagger
 * components:
 *   schemas:
 *     GetRating:
 *       type: object
 *       properties:
 *         userId:
 *           type: string
 *           format: uuid
 *         productId:
 *           type: string
 *           format: uuid
 *         rating:
 *           type: number
 *         comment:
 *           type: string
 *         isAccepted:
 *           type: string
 *         createdAt:
 *           type: string
 *         updatedAt:
 *           type: string
 */
