/**
 * @swagger
 * components:
 *   schemas:
 *     UpdateRating:
 *       type: object
 *       required:
 *       properties:
 *         userId:
 *           type: string
 *           format: uuid
 *         productId:
 *           type: string
 *           format: uuid
 *         rating:
 *           type: number
 *         comment:
 *           type: string
 *         isAccepted:
 *           type: string
 */
