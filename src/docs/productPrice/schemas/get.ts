/**
 * @swagger
 * components:
 *   schemas:
 *     GetProductPrice:
 *       type: object
 *       properties:
 *         product:
 *           type: string
 *           format: uuid
 *         price:
 *           type: number
 *         isEnabled:
 *           type: boolean
 *         createdAt:
 *           type: string
 *         updatedAt:
 *           type: string
 */
