/**
 * @swagger
 * components:
 *   schemas:
 *     VerifyPromoCode:
 *       type: object
 *       required:
 *         - code
 *       properties:
 *         userId:
 *           type: string
 *           format: uuid
 *         browserId:
 *           type: string
 *         code:
 *           type: string
 */
