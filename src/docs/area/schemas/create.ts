/**
 * @swagger
 * components:
 *   schemas:
 *     CreateArea:
 *       type: object
 *       required:
 *         - deliveryPrice
 *         - nameAng
 *         - nameAr
 *         - governorateId
 *       properties:
 *         deliveryPrice:
 *           type: integer
 *         nameAng:
 *           type: string
 *         nameAr:
 *           type: string
 *         governorateId:
 *           type: string
 *           format: objectId
 */
