/**
 * @swagger
 * components:
 *   schemas:
 *     GetArea:
 *       type: object
 *       properties:
 *         deliveryPrice:
 *           type: integer
 *         nameAng:
 *           type: string
 *         nameAr:
 *           type: string
 *         governorateId:
 *           type: string
 *           format: objectId
 *         createdAt:
 *           type: string
 *         updatedAt:
 *           type: string
 */
