/**
 * @swagger
 * components:
 *   schemas:
 *     RemoveFromCart:
 *       type: object
 *       required:
 *         - itemId
 *       properties:
 *         userId:
 *           type: string
 *           format: uuid
 *         browserId:
 *           type: string
 *         itemId:
 *           type: string
 *           format: uuid
 */
