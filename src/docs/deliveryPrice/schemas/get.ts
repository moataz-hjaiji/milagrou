/**
 * @swagger
 * components:
 *   schemas:
 *     GetDeliveryPrice:
 *       type: object
 *       properties:
 *         price:
 *           type: number
 *         isActive:
 *           type: boolean
 *         freeDeliveryOption:
 *           type: boolean
 *         freeAfter:
 *           type: number
 *         createdAt:
 *           type: string
 *         updatedAt:
 *           type: string
 */
