/**
 * @swagger
 * components:
 *   schemas:
 *     CreateDeliveryPrice:
 *       type: object
 *       required:
 *         - price
 *         - isActive
 *         - freeDeliveryOption
 *       properties:
 *         price:
 *           type: number
 *         isActive:
 *           type: boolean
 *         freeDeliveryOption:
 *           type: boolean
 *         freeAfter:
 *           type: number
 */
