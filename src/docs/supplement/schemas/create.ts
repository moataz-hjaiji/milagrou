/**
 * @swagger
 * components:
 *   schemas:
 *     CreateSupplement:
 *       type: object
 *       required:
 *         - nameAng
 *         - nameAr
 *         - image
 *       properties:
 *         nameAng:
 *           type: string
 *         nameAr:
 *           type: string
 *         image:
 *           type: string
 *           format: binary
 */
