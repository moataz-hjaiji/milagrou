/**
 * @swagger
 * components:
 *   schemas:
 *     subscribeOrUnsubscribe:
 *       type: object
 *       required:
 *         - topic
 *         - token
 *       properties:
 *         topic:
 *           type: string
 *         token:
 *           type: string
 */
