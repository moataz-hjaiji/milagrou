/**
 * @swagger
 * components:
 *   schemas:
 *     GetNotification:
 *       type: object
 *       properties:
 *         userId:
 *           type: string
 *         title:
 *           type: string
 *         body:
 *           type: string
 *         data:
 *           type: object
 *         isRead:
 *           type: boolean
 *         isSeen:
 *           type: boolean
 *         createdAt:
 *           type: string
 *         updatedAt:
 *           type: string
 *
 */
