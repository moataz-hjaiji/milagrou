/**
 * @swagger
 * components:
 *   schemas:
 *     DateRange:
 *       type: object
 *       required:
 *         - startDate
 *         - endDate
 *         - types
 *       properties:
 *         startDate:
 *           type: string
 *         endDate:
 *           type: string
 *         types:
 *           type: array
 *           items:
 *             type: string
 */
