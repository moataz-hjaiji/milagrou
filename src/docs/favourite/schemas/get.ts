/**
 * @swagger
 * components:
 *   schemas:
 *     GetFavourite:
 *       type: object
 *       properties:
 *         userId:
 *           type: string
 *           format: uuid
 *         product:
 *           type: string
 *           format: uuid
 *         createdAt:
 *           type: string
 *         updatedAt:
 *           type: string
 */
