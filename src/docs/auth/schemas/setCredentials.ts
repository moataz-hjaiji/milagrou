/**
 * @swagger
 * components:
 *   schemas:
 *     SetCredentials:
 *       type: object
 *       required:
 *         - phoneNumber
 *         - firstName
 *         - lastName
 *         - email
 *         - password
 *         - confirmPassword
 *       properties:
 *         phoneNumber:
 *           type: string
 *         registerConfirmationCode:
 *           type: number
 *         firstName:
 *           type: string
 *         lastName:
 *           type: string
 *         email:
 *           type: string
 *         password:
 *           type: string
 *         confirmPassword:
 *           type: string
 */
