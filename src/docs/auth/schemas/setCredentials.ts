/**
 * @swagger
 * components:
 *   schemas:
 *     SetCredentials:
 *       type: object
 *       required:
 *         - phoneNumber
 *         - userName
 *         - password
 *         - confirmPassword
 *       properties:
 *         phoneNumber:
 *           type: string
 *         registerConfirmationCode:
 *           type: number
 *         userName:
 *           type: string
 *         password:
 *           type: string
 *         confirmPassword:
 *           type: string
 */
